[Unit]
Description=UpStat Outreach — pipeline diário (scrape→personalize→auto-approve→send, só 1º email)
After=network.target

[Service]
Type=oneshot
User=upstat
Group=upstat
WorkingDirectory=/opt/upstat-outreach/python
# Mesmo cache de browser do serviço web; necessário pro scrape do pipeline.
Environment=PLAYWRIGHT_BROWSERS_PATH=/opt/upstat-outreach/.playwright
# Email que recebe o relatório do run (vazio/removido = não envia relatório):
Environment=REPORT_TO=dev.yagofontanez@gmail.com
# Ajuste o teto/dia e a lista de clientes aqui se quiser:
#Environment=PIPELINE_LIMIT=20
#Environment=PIPELINE_CLIENTS=upstat martinsadviser
ExecStart=/opt/upstat-outreach/deploy/pipeline.sh

# Hardening básico (igual ao serviço web)
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ReadWritePaths=/opt/upstat-outreach
